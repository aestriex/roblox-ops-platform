<div class="max-w-lg mx-auto space-y-6">
  <h1 class="text-2xl font-bold text-foreground">Edit <%= @work_item.title %></h1>

  <%= turbo_frame_tag @work_item.dialog_form_id do %>
    <%= render "workspace/work_items/form", project: @project, feature: @feature, deliverable: @deliverable, work_item: @work_item, assignees: @assignees %>
  <% end %>
</div>
